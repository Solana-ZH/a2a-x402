[project]
name = "ap2-demo"
version = "0.1.0"
description = "A demo showcasing AP2 and x402 for human-in-the-loop agent payments."
requires-python = ">=3.11"
dependencies = [
    "ap2", # Now referenced via tool.uv.sources
    "x402-a2a", # Now referenced via tool.uv.sources
    "starlette",
    "uvicorn",
    "pydantic",
    "eth-account",
    "web3",
    "httpx",
    "google-adk",
    "click",
    "flask",
    "requests",
    "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["server", "client"]

[project.scripts]
server = "server.__main__:main"

[tool.uv.workspace]
members = ["client"]

[tool.uv.sources]
x402-a2a = { path = "../../../python/x402_a2a", editable = true }

[tool.ruff]
target-version = "py313"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true

[project.optional-dependencies]
lint = [
    "ruff>=0.13.1",
]
typecheck = [
    "mypy>=1.18.0",
    "typing-extensions>=4.12.0",
]
